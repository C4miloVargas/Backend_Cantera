{
    "build": {
      "builder": "nixpacks",
      "buildCommand": "composer install --no-dev && php artisan key:generate --force && php artisan config:cache && php artisan route:cache"
    },
    "deploy": {
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 3
    }
  }