{
    "build": {
      "builder": "nixpacks",
      "buildCommand": "composer install --no-dev"
    },
    "deploy": {
      "startCommand": "php artisan key:generate --force && php artisan config:cache && php artisan route:cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT",
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 3
    }
  }